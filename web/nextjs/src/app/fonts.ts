import { Inter } from "next/font/google";

export const interBody = Inter({ subsets: ["latin"], variable: "--font-body" });
export const interMono = Inter({ subsets: ["latin"], variable: "--font-mono" });
export const interTitle = Inter({ subsets: ["latin"], variable: "--font-title" });
